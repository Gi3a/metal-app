import React from 'react'
import { useTitle } from '../utils/useTitle';

function AddOrder() {
	useTitle('AddOrder');

	return (
		<div className='page'>
			AddOrder
		</div>
	)
}

export default AddOrder