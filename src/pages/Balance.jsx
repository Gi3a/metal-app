import React from 'react'
import { useTitle } from '../utils/useTitle';

function Balance() {
	useTitle('Balance');

	return (
		<div className='page'>
			Balance
		</div>
	)
}

export default Balance