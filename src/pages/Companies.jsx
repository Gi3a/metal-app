import React from 'react'
import { useTitle } from '../utils/useTitle';

function Companies() {
	useTitle('Companies');

	return (
		<div className='page'>
			Companies
		</div>
	)
}

export default Companies