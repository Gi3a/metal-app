import React from 'react'
import { useTitle } from '../utils/useTitle';

function PersonalData() {
	useTitle('PersonalData');

	return (
		<div className='page'>
			PersonalData
		</div>
	)
}

export default PersonalData